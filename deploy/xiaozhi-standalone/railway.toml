[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/xiaozhi-standalone/Dockerfile"
watchPatterns = ["deploy/xiaozhi-standalone/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

import fs from 'node:fs';

const toolsPath = 'lib/browser-nubo-tools-line.ts';
let source = fs.readFileSync(toolsPath, 'utf8');

if (!source.includes('NUBO_MAPS_WEB_OVERLAY_V1')) {
  const helperAnchor = `function normalizeAppName(value: unknown) {`;
  const helpers = `// NUBO_MAPS_WEB_OVERLAY_V1\n// Google Maps only: keep the NUBO page, microphone and realtime voice session\n// alive while a full-screen Google Maps web layer is displayed above it.\n// No YouTube, Google Home, Gmail, wake-word, voice model or other tool behavior\n// is changed by this patch.\nconst NUBO_MAP_WEB_APP_NAMES = new Set([\n  \"maps\",\n  \"googlemaps\",\n  \"地圖\",\n  \"google地圖\",\n]);\n\nconst NUBO_MAP_OVERLAY_ID = \"nubo-google-maps-web-overlay-v1\";\nconst NUBO_MAP_FRAME_ID = \"nubo-google-maps-web-frame-v1\";\n\nfunction isPureWebNuboRuntime() {\n  if (typeof window === \"undefined\" || typeof navigator === \"undefined\") return false;\n  try {\n    const bridge = (window as typeof window & {\n      NuboNative?: { isNativeApp?: () => boolean };\n    }).NuboNative;\n    if (bridge?.isNativeApp?.() === true) return false;\n  } catch {}\n  return /Android|iPhone|iPad|iPod|Mobile/i.test(navigator.userAgent || \"\")\n    || window.matchMedia(\"(pointer: coarse) and (max-width: 1100px)\").matches;\n}\n\nfunction normalizeNuboMapApp(value: unknown) {\n  return String(value ?? \"\")\n    .trim()\n    .toLowerCase()\n    .replace(/[\\s_-]+/g, \"\");\n}\n\nfunction readBrowserPosition() {\n  return new Promise<{ latitude: number; longitude: number } | null>((resolve) => {\n    if (typeof navigator === \"undefined\" || !navigator.geolocation) {\n      resolve(null);\n      return;\n    }\n\n    let settled = false;\n    const finish = (value: { latitude: number; longitude: number } | null) => {\n      if (settled) return;\n      settled = true;\n      resolve(value);\n    };\n\n    navigator.geolocation.getCurrentPosition(\n      (position) => finish({\n        latitude: position.coords.latitude,\n        longitude: position.coords.longitude,\n      }),\n      () => finish(null),\n      {\n        enableHighAccuracy: false,\n        timeout: 2500,\n        maximumAge: 300000,\n      },\n    );\n  });\n}\n\nasync function buildNuboMapsEmbedUrl(queryValue: unknown, locationValue?: unknown) {\n  const query = String(queryValue ?? \"\").trim();\n  const location = String(locationValue ?? \"\").trim();\n\n  let searchText = [query, location].filter(Boolean).join(\" \").trim();\n\n  if (query && !location) {\n    const position = await readBrowserPosition();\n    if (position) {\n      searchText = query + \" near \" + position.latitude.toFixed(6) + \",\" + position.longitude.toFixed(6);\n    }\n  }\n\n  if (!searchText) {\n    return \"https://www.google.com/maps?output=embed\";\n  }\n\n  return \"https://www.google.com/maps?q=\" + encodeURIComponent(searchText) + \"&output=embed\";\n}\n\nfunction ensureNuboMapsOverlay() {\n  if (typeof document === \"undefined\") return null;\n\n  const existingOverlay = document.getElementById(NUBO_MAP_OVERLAY_ID) as HTMLDivElement | null;\n  const existingFrame = document.getElementById(NUBO_MAP_FRAME_ID) as HTMLIFrameElement | null;\n  if (existingOverlay && existingFrame) {\n    existingOverlay.style.display = \"block\";\n    return { overlay: existingOverlay, frame: existingFrame };\n  }\n\n  existingOverlay?.remove();\n\n  const overlay = document.createElement(\"div\");\n  overlay.id = NUBO_MAP_OVERLAY_ID;\n  Object.assign(overlay.style, {\n    position: \"fixed\",\n    inset: \"0\",\n    zIndex: \"2147483646\",\n    background: \"#ffffff\",\n    margin: \"0\",\n    padding: \"0\",\n    overflow: \"hidden\",\n  });\n\n  const frame = document.createElement(\"iframe\");\n  frame.id = NUBO_MAP_FRAME_ID;\n  frame.title = \"Google Maps\";\n  frame.setAttribute(\"allow\", \"geolocation *; fullscreen *\");\n  frame.setAttribute(\"referrerpolicy\", \"no-referrer-when-downgrade\");\n  Object.assign(frame.style, {\n    position: \"absolute\",\n    inset: \"0\",\n    width: \"100%\",\n    height: \"100%\",\n    border: \"0\",\n    margin: \"0\",\n    padding: \"0\",\n    background: \"#ffffff\",\n  });\n\n  const closeButton = document.createElement(\"button\");\n  closeButton.type = \"button\";\n  closeButton.setAttribute(\"aria-label\", \"返回 NUBO\");\n  closeButton.textContent = \"‹\";\n  Object.assign(closeButton.style, {\n    position: \"absolute\",\n    top: \"max(10px, env(safe-area-inset-top))\",\n    left: \"12px\",\n    zIndex: \"2\",\n    width: \"44px\",\n    height: \"44px\",\n    borderRadius: \"22px\",\n    border: \"1px solid rgba(0,0,0,.14)\",\n    background: \"rgba(255,255,255,.94)\",\n    color: \"#202124\",\n    fontSize: \"34px\",\n    lineHeight: \"38px\",\n    textAlign: \"center\",\n    boxShadow: \"0 2px 8px rgba(0,0,0,.18)\",\n  });\n  closeButton.addEventListener(\"click\", () => {\n    overlay.style.display = \"none\";\n  });\n\n  overlay.appendChild(frame);\n  overlay.appendChild(closeButton);\n  document.body.appendChild(overlay);\n  return { overlay, frame };\n}\n\nasync function showNuboMapsWebOverlay(queryValue: unknown, locationValue?: unknown) {\n  const holder = ensureNuboMapsOverlay();\n  if (!holder) return null;\n\n  const query = String(queryValue ?? \"\").trim();\n  const location = String(locationValue ?? \"\").trim();\n  const targetUrl = await buildNuboMapsEmbedUrl(query, location);\n\n  holder.frame.src = targetUrl;\n  holder.overlay.style.display = \"block\";\n\n  try {\n    window.localStorage.setItem(\"nubo_voice_auto_resume_v1\", \"true\");\n  } catch {}\n\n  return {\n    ok: true,\n    opened: true,\n    provider: \"Google Maps\",\n    query,\n    location: location || \"目前位置\",\n    url: targetUrl,\n    mode: \"same-page-maps-web-overlay\",\n    mapsOverlay: true,\n    preserveNubo: true,\n    nuboVoiceKeepAlive: true,\n    autoOpen: false,\n    supported: true,\n    build: \"maps-same-page-web-overlay-v1-20260820\",\n  };\n}\n\nasync function tryNuboMapsWebOverlay(call: FunctionCall) {\n  if (!isPureWebNuboRuntime()) return null;\n\n  const args = call.args ?? {};\n\n  if (call.name === \"search_nearby\") {\n    const query = String(args.query ?? \"\").trim();\n    if (!query) return null;\n    return showNuboMapsWebOverlay(query, args.location);\n  }\n\n  if (call.name !== \"open_mobile_app\") return null;\n  const app = normalizeNuboMapApp(args.app);\n  if (!NUBO_MAP_WEB_APP_NAMES.has(app)) return null;\n\n  return showNuboMapsWebOverlay(args.query, undefined);\n}\n\n`;

  if (!source.includes(helperAnchor)) {
    throw new Error('maps same-page overlay: helper anchor missing');
  }
  source = source.replace(helperAnchor, helpers + helperAnchor);

  const executeAnchor = `export async function executeNuboBrowserTool(call: FunctionCall) {`;
  const executePatch = `${executeAnchor}\n  const mapsWebOverlayResult = await tryNuboMapsWebOverlay(call);\n  if (mapsWebOverlayResult) return mapsWebOverlayResult;`;

  if (!source.includes(executeAnchor)) {
    throw new Error('maps same-page overlay: execute anchor missing');
  }
  source = source.replace(executeAnchor, executePatch);
}

fs.writeFileSync(toolsPath, source);
console.log('Applied Google Maps same-page full-screen web overlay only');

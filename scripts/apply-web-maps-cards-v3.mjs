import fs from 'node:fs';

const path = 'lib/browser-nubo-tools-line.ts';
let source = fs.readFileSync(path, 'utf8');

if (!source.includes('NUBO_MAPS_CARD_LIST_V3')) {
  if (!source.includes('NUBO_MAPS_WEB_OVERLAY_V2_LIST')) {
    throw new Error('maps cards v3: V2 map overlay must be applied first');
  }

  source = source.replace(
    '// NUBO_MAPS_WEB_OVERLAY_V2_LIST',
    '// NUBO_MAPS_WEB_OVERLAY_V2_LIST\n// NUBO_MAPS_CARD_LIST_V3',
  );

  const typeAnchor = `type NuboMapPlaceResult = {\n  name?: string;\n  category?: string;\n  address?: string;\n  lat?: number;\n  lng?: number;\n  distanceMeters?: number;\n  mapsUrl?: string;\n};`;
  const typeReplacement = `type NuboMapPlaceResult = {\n  name?: string;\n  category?: string;\n  address?: string;\n  lat?: number;\n  lng?: number;\n  distanceMeters?: number;\n  mapsUrl?: string;\n  imageUrl?: string;\n  website?: string;\n  rating?: number;\n  userRatingCount?: number;\n  provider?: \"google\" | \"osm\";\n  photoAttribution?: string;\n};`;
  if (!source.includes(typeAnchor)) {
    throw new Error('maps cards v3: NuboMapPlaceResult type anchor missing');
  }
  source = source.replace(typeAnchor, typeReplacement);

  source = source.replace('timeout: 2500,', 'timeout: 1200,');
  source = source.replace(
    'limit: 10,\n        radiusMeters: 2500,',
    'limit: 8,\n        radiusMeters: 1800,',
  );

  const oldImmediate = `  const position = location ? null : await readBrowserPosition();\n  const targetUrl = buildNuboMapsEmbedUrl(query, location, position);\n  const serial = ++nuboMapsSearchSerial;\n\n  holder.frame.src = targetUrl;\n  holder.overlay.style.display = \"block\";`;
  const newImmediate = `  // Paint the map immediately. Current-position refinement happens afterward,\n  // so a geolocation prompt can no longer make the whole Maps screen feel frozen.\n  const preliminaryUrl = buildNuboMapsEmbedUrl(query, location, null);\n  holder.frame.src = preliminaryUrl;\n  holder.overlay.style.display = \"block\";\n\n  const position = location ? null : await readBrowserPosition();\n  const targetUrl = buildNuboMapsEmbedUrl(query, location, position);\n  const serial = ++nuboMapsSearchSerial;\n  if (location || position) holder.frame.src = targetUrl;`;
  if (!source.includes(oldImmediate)) {
    throw new Error('maps cards v3: immediate map anchor missing');
  }
  source = source.replace(oldImmediate, newImmediate);

  const renderPattern = /    results\.forEach\(\(place, index\) => \{[\s\S]*?      holder\.listBody\.appendChild\(button\);\n    \}\);/;
  if (!renderPattern.test(source)) {
    throw new Error('maps cards v3: list render block missing');
  }

  const renderReplacement = `    results.forEach((place, index) => {\n      const placeName = String(place.name ?? \"未命名店家\");\n      const placeCategory = String(place.category ?? \"\");\n      const placeAddress = String(place.address ?? \"\");\n      const placeDistance = Number(place.distanceMeters);\n      const placeLat = Number(place.lat);\n      const placeLng = Number(place.lng);\n      const placeRating = Number(place.rating);\n      const placeRatingCount = Number(place.userRatingCount);\n      const vegetarianResult = /素食|蔬食|純素|全素|奶蛋素|蛋奶素|vegan|vegetarian|plant\\s*-?\\s*based/i.test(\n        query + \" \" + placeName + \" \" + placeCategory,\n      );\n\n      const card = document.createElement(\"div\");\n      Object.assign(card.style, {\n        borderBottom: \"1px solid #e5e7eb\",\n        padding: \"10px 11px 9px\",\n        background: \"#ffffff\",\n        color: \"#111827\",\n        opacity: \"1\",\n      });\n\n      const button = document.createElement(\"button\");\n      button.type = \"button\";\n      Object.assign(button.style, {\n        display: \"grid\",\n        gridTemplateColumns: \"92px minmax(0, 1fr)\",\n        gap: \"11px\",\n        width: \"100%\",\n        border: \"0\",\n        padding: \"0\",\n        margin: \"0\",\n        appearance: \"none\",\n        WebkitAppearance: \"none\",\n        background: \"transparent\",\n        color: \"#111827\",\n        opacity: \"1\",\n        textAlign: \"left\",\n        cursor: \"pointer\",\n      });\n\n      const media = document.createElement(\"div\");\n      Object.assign(media.style, {\n        position: \"relative\",\n        width: \"92px\",\n        height: \"72px\",\n        borderRadius: \"10px\",\n        overflow: \"hidden\",\n        background: vegetarianResult ? \"#edf7ef\" : \"#f3f4f6\",\n        border: \"1px solid #e5e7eb\",\n      });\n\n      const fallback = document.createElement(\"div\");\n      fallback.textContent = vegetarianResult ? \"🥬\" : \"📍\";\n      Object.assign(fallback.style, {\n        position: \"absolute\",\n        inset: \"0\",\n        display: \"grid\",\n        placeItems: \"center\",\n        fontSize: vegetarianResult ? \"32px\" : \"28px\",\n        lineHeight: \"1\",\n        background: vegetarianResult ? \"#edf7ef\" : \"#f3f4f6\",\n      });\n      media.appendChild(fallback);\n\n      if (place.imageUrl) {\n        const image = document.createElement(\"img\");\n        image.src = place.imageUrl;\n        image.alt = placeName + \" 圖片\";\n        image.loading = index < 3 ? \"eager\" : \"lazy\";\n        image.decoding = \"async\";\n        Object.assign(image.style, {\n          position: \"absolute\",\n          inset: \"0\",\n          width: \"100%\",\n          height: \"100%\",\n          objectFit: \"cover\",\n          opacity: \"1\",\n        });\n        image.addEventListener(\"error\", () => image.remove());\n        media.appendChild(image);\n      }\n\n      const number = document.createElement(\"span\");\n      number.textContent = String(index + 1);\n      Object.assign(number.style, {\n        position: \"absolute\",\n        left: \"5px\",\n        top: \"5px\",\n        minWidth: \"22px\",\n        height: \"22px\",\n        padding: \"0 6px\",\n        borderRadius: \"11px\",\n        background: \"rgba(255,255,255,.96)\",\n        color: \"#111827\",\n        fontSize: \"12px\",\n        lineHeight: \"22px\",\n        textAlign: \"center\",\n        fontWeight: \"800\",\n        boxShadow: \"0 1px 4px rgba(0,0,0,.16)\",\n      });\n      media.appendChild(number);\n\n      const content = document.createElement(\"div\");\n      Object.assign(content.style, { minWidth: \"0\", color: \"#111827\" });\n\n      const name = document.createElement(\"strong\");\n      name.textContent = placeName;\n      Object.assign(name.style, {\n        display: \"block\",\n        color: \"#111827\",\n        opacity: \"1\",\n        fontSize: \"15px\",\n        fontWeight: \"800\",\n        lineHeight: \"1.32\",\n        whiteSpace: \"nowrap\",\n        overflow: \"hidden\",\n        textOverflow: \"ellipsis\",\n      });\n      content.appendChild(name);\n\n      if (Number.isFinite(placeRating) && placeRating > 0) {\n        const rating = document.createElement(\"div\");\n        const countText = Number.isFinite(placeRatingCount) && placeRatingCount > 0\n          ? \"（\" + Math.round(placeRatingCount) + \"）\"\n          : \"\";\n        rating.textContent = \"★ \" + placeRating.toFixed(1) + countText;\n        Object.assign(rating.style, {\n          marginTop: \"4px\",\n          color: \"#b06000\",\n          fontSize: \"12px\",\n          fontWeight: \"700\",\n          lineHeight: \"1.3\",\n        });\n        content.appendChild(rating);\n      }\n\n      const meta = document.createElement(\"div\");\n      const distance = Number.isFinite(placeDistance)\n        ? (placeDistance < 1000\n          ? Math.round(placeDistance) + \" m\"\n          : (placeDistance / 1000).toFixed(1) + \" km\")\n        : \"\";\n      meta.textContent = [distance, placeCategory, placeAddress].filter(Boolean).join(\" · \ ");\n      Object.assign(meta.style, {\n        marginTop: \"5px\",\n        color: \"#374151\",\n        opacity: \"1\",\n        fontSize: \"13px\",\n        fontWeight: \"500\",\n        lineHeight: \"1.38\",\n        display: \"-webkit-box\",\n        WebkitLineClamp: \"2\",\n        WebkitBoxOrient: \"vertical\",\n        overflow: \"hidden\",\n      });\n      content.appendChild(meta);\n\n      button.appendChild(media);\n      button.appendChild(content);\n\n      button.addEventListener(\"click\", () => {\n        if (Number.isFinite(placeLat) && Number.isFinite(placeLng)) {\n          holder.frame.src = buildNuboMapsEmbedUrl(placeName, location, {\n            latitude: placeLat,\n            longitude: placeLng,\n          });\n        } else {\n          holder.frame.src = buildNuboMapsEmbedUrl(placeName, location, null);\n        }\n      });\n      card.appendChild(button);\n\n      const links = document.createElement(\"div\");\n      Object.assign(links.style, {\n        display: \"flex\",\n        alignItems: \"center\",\n        flexWrap: \"wrap\",\n        gap: \"12px\",\n        marginTop: \"8px\",\n        paddingLeft: \"103px\",\n        fontSize: \"13px\",\n        lineHeight: \"1.3\",\n      });\n\n      const styleLink = (link: HTMLAnchorElement) => {\n        Object.assign(link.style, {\n          color: \"#1a73e8\",\n          opacity: \"1\",\n          fontWeight: \"700\",\n          textDecoration: \"none\",\n        });\n      };\n\n      const mapsLink = document.createElement(\"a\");\n      const fallbackMapsUrl =\n        \"https://www.google.com/maps/search/?api=1&query=\" +\n        encodeURIComponent(placeName);\n      mapsLink.href = String(place.mapsUrl ?? fallbackMapsUrl);\n      mapsLink.target = \"_blank\";\n      mapsLink.rel = \"noopener noreferrer\";\n      mapsLink.textContent = \"Google Maps ↗\";\n      styleLink(mapsLink);\n      links.appendChild(mapsLink);\n\n      if (place.website) {\n        const websiteLink = document.createElement(\"a\");\n        websiteLink.href = place.website;\n        websiteLink.target = \"_blank\";\n        websiteLink.rel = \"noopener noreferrer\";\n        websiteLink.textContent = \"官網 ↗\";\n        styleLink(websiteLink);\n        links.appendChild(websiteLink);\n      }\n\n      if (place.photoAttribution) {\n        const attribution = document.createElement(\"span\");\n        attribution.textContent = \"照片：\" + place.photoAttribution;\n        Object.assign(attribution.style, {\n          color: \"#6b7280\",\n          fontSize: \"10px\",\n          fontWeight: \"400\",\n        });\n        links.appendChild(attribution);\n      }\n\n      card.appendChild(links);\n      holder.listBody.appendChild(card);\n    });`;

  source = source.replace(renderPattern, renderReplacement);
  fs.writeFileSync(path, source);
}

console.log('Applied Maps V3 precise image cards, ratings and clearer contrast');

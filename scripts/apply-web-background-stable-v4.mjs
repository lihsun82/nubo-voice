import fs from 'node:fs';

const path = 'lib/mobile-direct-app-v4.ts';
let source = fs.readFileSync(path, 'utf8');

if (!source.includes('NUBO_WEB_BACKGROUND_STABLE_V4')) {
  const helperAnchor = `function openInSeparateContext(url: string, targetName: string) {`;
  const helpers = `// NUBO_WEB_BACKGROUND_STABLE_V4\n// Restore the pre-APK Mobile Direct App V4 behavior for ordinary mobile browsers.\n// Android Chrome launches supported apps through intent:// from the existing NUBO tab,\n// so the NUBO page remains alive behind the external app instead of being replaced.\nfunction isNativeNuboRuntime() {\n  if (typeof window === \"undefined\") return false;\n  const bridge = (window as NuboNativeWindow).NuboNative;\n  if (!bridge) return false;\n  try {\n    return bridge.isNativeApp?.() === true;\n  } catch {\n    return false;\n  }\n}\n\nfunction androidWebIntent(\n  intentPath: string,\n  scheme: string,\n  packageName: string,\n  fallbackUrl: string,\n) {\n  return (\n    \`intent://\${intentPath}\` +\n    \`#Intent;scheme=\${scheme};package=\${packageName};\` +\n    \`S.browser_fallback_url=\${encodeURIComponent(fallbackUrl)};end\`\n  );\n}\n\nfunction openWebFallbackPreservingNubo(url: string) {\n  try {\n    const external = window.open(url, \"_blank\", \"noopener,noreferrer\");\n    if (external) return true;\n  } catch {}\n  return false;\n}\n\nfunction launchLegacyWebBackgroundV4(\n  targetUrl: string,\n  label: string,\n  preferMusic: boolean,\n) {\n  if (typeof window === \"undefined\" || !isAndroid() || isNativeNuboRuntime()) {\n    return null;\n  }\n\n  const normalizedLabel = label.toLowerCase();\n  const normalizedUrl = targetUrl.toLowerCase();\n  let intentUrl = \"\";\n\n  if (isYouTubeUrl(targetUrl)) {\n    const videoId = extractYouTubeVideoId(targetUrl);\n    if (preferMusic) {\n      const path = videoId\n        ? \`music.youtube.com/watch?v=\${encodeURIComponent(videoId)}\`\n        : \"music.youtube.com/\";\n      intentUrl = androidWebIntent(\n        path,\n        \"https\",\n        \"com.google.android.apps.youtube.music\",\n        targetUrl,\n      );\n    } else {\n      const path = videoId\n        ? \`www.youtube.com/watch?v=\${encodeURIComponent(videoId)}\`\n        : \"www.youtube.com/\";\n      intentUrl = androidWebIntent(\n        path,\n        \"https\",\n        \"com.google.android.youtube\",\n        targetUrl,\n      );\n    }\n  } else if (normalizedLabel === \"facebook\" || normalizedUrl.includes(\"facebook.com\") || normalizedUrl.includes(\"fb.com\")) {\n    intentUrl = androidWebIntent(\n      \"www.facebook.com/\",\n      \"https\",\n      \"com.facebook.katana\",\n      targetUrl,\n    );\n  } else if (normalizedLabel === \"instagram\" || normalizedUrl.includes(\"instagram.com\")) {\n    intentUrl = androidWebIntent(\n      \"instagram.com/\",\n      \"https\",\n      \"com.instagram.android\",\n      targetUrl,\n    );\n  } else if (normalizedLabel === \"line\" || normalizedUrl.includes(\"line.me/\")) {\n    intentUrl = androidWebIntent(\n      \"nv/chat\",\n      \"line\",\n      \"jp.naver.line.android\",\n      targetUrl,\n    );\n  }\n\n  if (!intentUrl) {\n    const opened = openWebFallbackPreservingNubo(targetUrl);\n    return {\n      opened,\n      mode: opened ? \"web-background-new-tab\" : \"web-background-popup-blocked\",\n      launchedUrl: opened ? targetUrl : \"\",\n      fallbackUrl: targetUrl,\n      label,\n    };\n  }\n\n  let backgrounded = false;\n  let fallbackTimer = 0;\n  const onVisibility = () => {\n    if (document.visibilityState === \"hidden\") {\n      backgrounded = true;\n      if (fallbackTimer) window.clearTimeout(fallbackTimer);\n    }\n  };\n  document.addEventListener(\"visibilitychange\", onVisibility, { once: true });\n\n  fallbackTimer = window.setTimeout(() => {\n    if (!backgrounded && document.visibilityState === \"visible\") {\n      openWebFallbackPreservingNubo(targetUrl);\n    }\n  }, 2200);\n\n  try {\n    window.location.assign(intentUrl);\n  } catch {\n    openWebFallbackPreservingNubo(targetUrl);\n  }\n\n  return {\n    opened: true,\n    mode: \"web-background-legacy-v4-intent\",\n    launchedUrl: intentUrl,\n    fallbackUrl: targetUrl,\n    label,\n  };\n}\n\n`;

  if (!source.includes(helperAnchor)) {
    throw new Error('web background stable: helper anchor missing');
  }
  source = source.replace(helperAnchor, helpers + helperAnchor);

  const routeAnchor = `  const youtube = isYouTubeUrl(targetUrl);\n\n  if (youtube) {`;
  const routePatch = `  const youtube = isYouTubeUrl(targetUrl);\n\n  // Pure mobile-browser path: use the proven pre-APK V4 intent strategy.\n  // APK/WebView runtimes bypass this block and keep their current native bridge.\n  const legacyWebLaunch = launchLegacyWebBackgroundV4(\n    targetUrl,\n    label,\n    preferMusic,\n  );\n  if (legacyWebLaunch) {\n    return {\n      ...(result as Record<string, unknown>),\n      mobileUrl: undefined,\n      playerUrl: undefined,\n      autoOpen: false,\n      opened: legacyWebLaunch.opened,\n      mode: legacyWebLaunch.mode,\n      externalTab: legacyWebLaunch.mode === \"web-background-new-tab\",\n      nativeBridge: false,\n      forcedSameTab: false,\n      preserveNubo: true,\n      launchedUrl: legacyWebLaunch.launchedUrl,\n      fallbackUrl: legacyWebLaunch.fallbackUrl,\n      mobileLabel: label,\n      launchBlocked: !legacyWebLaunch.opened,\n      build: \"web-background-stable-v4-20260819\",\n    };\n  }\n\n  if (youtube) {`;

  if (!source.includes(routeAnchor)) {
    throw new Error('web background stable: route anchor missing');
  }
  source = source.replace(routeAnchor, routePatch);
}

fs.writeFileSync(path, source);
console.log('Applied NUBO Web Background Stable V4: legacy Android Chrome intent launch + preserve NUBO');

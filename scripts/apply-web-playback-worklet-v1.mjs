import fs from 'node:fs';

const path = 'lib/browser-audio.ts';
const marker = 'NUBO_AUDIOWORKLET_STREAM_V1';
let source = fs.readFileSync(path, 'utf8');

if (!source.includes(marker)) {
  const classPattern = /export class PcmPlaybackQueue \{[\s\S]*\n\}\s*$/;
  if (!classPattern.test(source)) {
    throw new Error('playback worklet: PcmPlaybackQueue class not found');
  }

  const replacement = `// ${marker}\nfunction shouldUseStreamingWorklet() {\n  if (typeof window === \"undefined\") return false;\n  return (\n    /Android|iPhone|iPad|iPod|Mobile/i.test(window.navigator.userAgent) ||\n    window.navigator.maxTouchPoints > 0 ||\n    window.matchMedia?.(\"(pointer: coarse)\")?.matches === true\n  );\n}\n\nexport class PcmPlaybackQueue {\n  private context: AudioContext | null = null;\n  private sources = new Set<AudioBufferSourceNode>();\n  private workletNode: AudioWorkletNode | null = null;\n  private workletInitPromise: Promise<void> | null = null;\n  private workletUnavailable = false;\n  private nextStart = 0;\n  private readonly scheduleLeadSeconds = 0.08;\n  private foregroundListenersAttached = false;\n  private retired = false;\n  private playbackActive = false;\n  private playbackTailTimer: number | null = null;\n\n  constructor() {\n    this.claimExclusiveOutput();\n  }\n\n  private setPlaybackActive(active: boolean) {\n    if (this.playbackActive === active) return;\n    this.playbackActive = active;\n    dispatchPlaybackState(active);\n  }\n\n  private clearPlaybackTailTimer() {\n    if (this.playbackTailTimer === null) return;\n    window.clearTimeout(this.playbackTailTimer);\n    this.playbackTailTimer = null;\n  }\n\n  private claimExclusiveOutput() {\n    if (activePlaybackQueue && activePlaybackQueue !== this) {\n      activePlaybackQueue.retire();\n    }\n    activePlaybackQueue = this;\n    this.retired = false;\n  }\n\n  private retire() {\n    if (this.retired) return;\n    this.retired = true;\n    this.detachForegroundListeners();\n    this.interrupt();\n    this.workletNode?.disconnect();\n    this.workletNode = null;\n    const staleContext = this.context;\n    this.context = null;\n    if (staleContext) void staleContext.close().catch(() => undefined);\n  }\n\n  private readonly handleForeground = () => {\n    if (document.visibilityState === \"visible\" && !this.retired) {\n      void this.resume();\n    }\n  };\n\n  private attachForegroundListeners() {\n    if (this.foregroundListenersAttached || this.retired) return;\n    addForegroundListeners(this.handleForeground);\n    this.foregroundListenersAttached = true;\n  }\n\n  private detachForegroundListeners() {\n    if (!this.foregroundListenersAttached) return;\n    removeForegroundListeners(this.handleForeground);\n    this.foregroundListenersAttached = false;\n  }\n\n  private async ensureWorklet(context: AudioContext) {\n    if (\n      this.retired ||\n      this.workletNode ||\n      this.workletUnavailable ||\n      !shouldUseStreamingWorklet() ||\n      !context.audioWorklet ||\n      typeof AudioWorkletNode === \"undefined\"\n    ) {\n      return;\n    }\n\n    if (!this.workletInitPromise) {\n      this.workletInitPromise = (async () => {\n        try {\n          await context.audioWorklet.addModule(\"/nubo-pcm-playback-worklet.js?v=2\");\n          if (this.retired || this.context !== context) return;\n\n          const node = new AudioWorkletNode(context, \"nubo-pcm-stream\", {\n            numberOfInputs: 0,\n            numberOfOutputs: 1,\n            outputChannelCount: [1],\n          });\n          node.port.onmessage = (event) => {\n            if (event.data?.type !== \"drained\") return;\n            dispatchVoiceLevel(0);\n            this.clearPlaybackTailTimer();\n            this.playbackTailTimer = window.setTimeout(() => {\n              this.playbackTailTimer = null;\n              if (!this.retired) this.setPlaybackActive(false);\n            }, 120);\n          };\n          node.connect(context.destination);\n          this.workletNode = node;\n          window.dispatchEvent(\n            new CustomEvent(\"nubo:audio-playback-mode\", {\n              detail: {\n                mode: \"audio-worklet-stream\",\n                sampleRate: context.sampleRate,\n              },\n            }),\n          );\n        } catch (error) {\n          console.warn(\"[NUBO audio] AudioWorklet playback unavailable; using fallback\", error);\n          this.workletUnavailable = true;\n          window.dispatchEvent(\n            new CustomEvent(\"nubo:audio-playback-mode\", {\n              detail: {\n                mode: \"buffer-source-fallback\",\n                sampleRate: context.sampleRate,\n              },\n            }),\n          );\n        }\n      })().finally(() => {\n        this.workletInitPromise = null;\n      });\n    }\n\n    await this.workletInitPromise;\n  }\n\n  private async ensureContext(sampleRate = 24000) {\n    if (this.retired) return null;\n\n    if (activePlaybackQueue !== this) this.claimExclusiveOutput();\n\n    if (!this.context) {\n      if (shouldUseStreamingWorklet()) {\n        try {\n          // Gemini Live output is PCM16 / 24 kHz. Matching the context rate keeps\n          // resampling state continuous at the context -> hardware boundary,\n          // instead of reinitializing a resampler for every network chunk.\n          this.context = new AudioContext({\n            latencyHint: \"playback\",\n            sampleRate,\n          });\n        } catch {\n          this.context = new AudioContext({ latencyHint: \"playback\" });\n        }\n      } else {\n        this.context = new AudioContext({ latencyHint: \"playback\" });\n      }\n      this.attachForegroundListeners();\n    }\n\n    const context = this.context;\n    if (!context) return null;\n\n    await this.ensureWorklet(context);\n    if (context.state === \"suspended\") {\n      await context.resume().catch(() => undefined);\n    }\n    return context;\n  }\n\n  async resume() {\n    if (this.retired || !this.context) return false;\n    if (this.context.state === \"suspended\") {\n      await this.context.resume().catch(() => undefined);\n    }\n    this.nextStart = Math.max(this.nextStart, this.context.currentTime);\n    return this.context.state === \"running\";\n  }\n\n  async enqueue(base64: string, sampleRate = 24000) {\n    if (this.retired) return;\n    const context = await this.ensureContext(sampleRate);\n    if (!context || this.retired || activePlaybackQueue !== this) return;\n\n    const bytes = fromBase64(base64);\n    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);\n    const sampleCount = Math.floor(bytes.byteLength / 2);\n    if (sampleCount <= 0) return;\n\n    const samples = new Float32Array(sampleCount);\n    for (let i = 0; i < sampleCount; i += 1) {\n      samples[i] = view.getInt16(i * 2, true) / 0x8000;\n    }\n\n    dispatchVoiceLevel(Math.min(1, calculateRms(samples) * 4.6));\n    this.clearPlaybackTailTimer();\n    this.setPlaybackActive(true);\n\n    if (this.workletNode) {\n      try {\n        this.workletNode.port.postMessage(\n          { type: \"push\", samples },\n          [samples.buffer],\n        );\n        return;\n      } catch {\n        this.workletUnavailable = true;\n        this.workletNode.disconnect();\n        this.workletNode = null;\n      }\n    }\n\n    // Compatibility fallback. When the 24 kHz context request succeeded this\n    // path also avoids per-chunk 24k -> 48k resampler resets.\n    const audioBuffer = context.createBuffer(1, sampleCount, sampleRate);\n    audioBuffer.copyToChannel(samples, 0);\n\n    const node = context.createBufferSource();\n    node.buffer = audioBuffer;\n    node.connect(context.destination);\n    const startAt = Math.max(\n      context.currentTime + this.scheduleLeadSeconds,\n      this.nextStart,\n    );\n    node.start(startAt);\n    this.nextStart = startAt + audioBuffer.duration;\n    this.sources.add(node);\n    node.onended = () => {\n      this.sources.delete(node);\n      if (this.sources.size === 0 && this.nextStart <= context.currentTime + 0.02) {\n        dispatchVoiceLevel(0);\n        this.clearPlaybackTailTimer();\n        this.playbackTailTimer = window.setTimeout(() => {\n          this.playbackTailTimer = null;\n          if (\n            !this.retired &&\n            this.sources.size === 0 &&\n            this.nextStart <= context.currentTime + 0.04\n          ) {\n            this.setPlaybackActive(false);\n          }\n        }, 180);\n      }\n    };\n  }\n\n  interrupt() {\n    this.clearPlaybackTailTimer();\n    try { this.workletNode?.port.postMessage({ type: \"clear\" }); } catch {}\n    for (const node of this.sources) {\n      try { node.stop(); } catch {}\n    }\n    this.sources.clear();\n    this.nextStart = this.context?.currentTime ?? 0;\n    dispatchVoiceLevel(0);\n    this.setPlaybackActive(false);\n  }\n\n  async close() {\n    this.detachForegroundListeners();\n    this.interrupt();\n    this.retired = true;\n    if (activePlaybackQueue === this) activePlaybackQueue = null;\n    const node = this.workletNode;\n    this.workletNode = null;\n    try { node?.disconnect(); } catch {}\n    const context = this.context;\n    this.context = null;\n    await context?.close().catch(() => undefined);\n  }\n}`;

  source = source.replace(classPattern, replacement);
  fs.writeFileSync(path, source);
}

const finalSource = fs.readFileSync(path, 'utf8');
for (const token of [marker, 'audio-worklet-stream', 'sampleRate,', 'nubo-pcm-playback-worklet.js']) {
  if (!finalSource.includes(token)) {
    throw new Error(`playback worklet verification failed: ${token}`);
  }
}

console.log('Applied continuous 24 kHz AudioWorklet playback stream');
